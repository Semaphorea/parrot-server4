{% extends 'base.html.twig' %}

{% block title %}Employee{% endblock %}

{% block body %}  <div class="maincontainer {{page}}">
    <h1>Employee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employee.id }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ employee.lastname }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ employee.firstname }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ employee.genre }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ employee.email }}</td>
            </tr>
        </tbody> 
    </table>

    <a href="{{ path('app_admin_employee') }}">back to list</a>

    <a href="{{ path('app_crud_employee_edit', {'id': employee.id}) }}">edit</a>

    {{ include('Crud/crud_employee/_delete_form.html.twig') }}
    </div>
{% endblock %}



{% block body %}
<div class="maincontainer {{page}}">
    <h1>Employee index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Lastname</th>
                <th>Firstname</th>
                <th>Genre</th>
                <th>Email</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for employee in employees %}
            <tr>
                <td>{{ employee.id }}</td>
                <td>{{ employee.lastname }}</td>
                <td>{{ employee.firstname }}</td>
                <td>{{ employee.genre }}</td>
                <td>{{ employee.email }}</td>  
                <td>
                    <a href="{{ path('app_administration', {'id': employee.id}) }}">show</a>
                    <a href="{{ path('app_crud_employee_edit', {'id': employee.id}) }}">edit</a>
                </td>
            </tr> 
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>  
    </table>

    <a href="{{ path('app_crud_employee_new') }}">Create new</a>
    </div>
{% endblock %}
